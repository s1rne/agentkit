---
description: Team roles. Cursor has no subagents — to work in a role, say "act as <role>" and follow its rules.
---

# Team roles

In Cursor roles are not isolated into separate contexts. To work in one, say it explicitly — "act as `critic`" — and follow its rules below. Parallel launches are unavailable: tasks run sequentially.

{{ROLES_DOC}}
