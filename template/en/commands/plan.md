---
description: Break work into features and tasks with acceptance criteria
---

Call `planner` to decompose: $ARGUMENTS

It must: read `tasks/README.md`, the `task-protocol` skill and the relevant docs; decompose per the decomposition rules; create files in `tasks/features/` and `tasks/tasks/` with frontmatter filled in; set `owner`, `reviewer`, `risk`, `touches`, `blocked_by`; update `tasks/BOARD.md`.

Show me the decomposition as a table and name what's blocked and on what.
