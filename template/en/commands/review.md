---
description: Adversarial review of changes or a task
---

Call `critic`: $ARGUMENTS

No argument — review uncommitted changes.

The critic works per `adversarial-review`: hunting defects, not approving. Every finding carries a concrete failure scenario or it doesn't count.

If permissions, personal data, or a public surface are involved, also call `security-auditor`.

Show findings ordered by consequence, plus the verdict.
